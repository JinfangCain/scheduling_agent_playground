# Do not put real secrets in this file.
# For Streamlit Community Cloud, paste secrets into the app's Secrets UI.

# Future OpenAI-backed mode can read either st.secrets["OPENAI_API_KEY"]
# or the OPENAI_API_KEY environment variable.
OPENAI_API_KEY = "replace_with_your_key_in_streamlit_cloud_secrets"
